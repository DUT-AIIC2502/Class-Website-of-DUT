## 数列极限

### 数列的定义

> @info
>
> **定义 1 ：** 定义在正整数集上，并按照正整数顺序排列的一串实数 $\{a_n\}_{n\in \mathbb{N}^*} = \{a_1, a_2, \cdots, a_n, \cdots\}$ 称为**数列**，通常用 $\{a_n\}(n \geq 1)$ 这样的记号来表示。第 $n$ 项 $a_n$ 称为这个数列的**通项**。
>
> 或者说，数列 $\{a_n\}$ 是正整数集 $\N_+$ 到实数集的一个映射，$n$ 的**像**就是 $a_n\in \mathbb{R}$。
>
> 根据实数和数轴上的点的对应，数列可以看成数轴上的一串点，所以也成数列为**点列**。
>
>
> **定义 2 ：**
>
> * 方法一：已知数列 $\{a_n\}$，设数列 $\{n_i\}(i=1,2,\cdots)$ 满足：① $n_i \geq i$；② $n_{i+1} > n_i$，则称数列 $\{a_{n_i} \} = \{ a_{n_1}, a_{n_2}, \cdots, a_{n_i}, \cdots \}$ 为数列 $\{a_n\}$ 的**子(数)列**。
>
>   * 通俗解释：只能往后挑。
> * 方法二：所谓一个数列 $\{a_n\}$ 的**子(数)列**，是指取自原数列 $\{a_n\}$ 中的<u>无穷多项</u>，按照原数列中同样的顺序写成的一个新的数列。于是 $\{a_n\}$ 的子列是这种形式：$\{a_{n_k}\}(k\geq1)$，其中 $n_k(k\geq1)$ 都是正整数，满足 $n_1<n_2<\cdots<n_k<\cdots$。

1. 数列定义的误区：

    1. 与集合的区别：数列以**下标**区分元素。
    2. 数列的项数：数列一定是==无穷的==，且下标一定是连续的，==不能少项==。

### 数列极限的定义

> @info
>
> **定义 1 ：** 设 $\{a_n\}$ 是给定的数列。如果有一个实数 $a$ 具有下列性质：$\forall \ \varepsilon > 0$，$\exists \ N=N(\varepsilon) \in \N$（即 $N$ 可能依赖于 $\varepsilon$），使得当 $n>N$ 时，不等式
>
> $$
> |a_n-a|<\varepsilon \Leftrightarrow a-\varepsilon<a_n<a+\varepsilon \Leftrightarrow 
> a \in U(A, \varepsilon)
> $$
>
> 成立，那么称 $a$ 是**数列**  **的极限**，或数列 $\{a_n\}$ 以 $a$ 为极限。也称数列 $\{a_n\}$ **收敛**于 $a$，记成
>
> $$
> \lim_{n \to \infty} a_n = a \ 或  \ \lim a_n = a,
> $$
>
> 也可以记成 $a_n \to a \ (n\to \infty)$，读作“当 $n$ 趋于无穷时，$a_n$ 趋于 $a$”。
>
> **定义 2 ：** 有极限的数列称为**收敛数列**，不收敛的数列称为**发散数列**。
>
> * 发散的定义：$\forall \ a \in \mathbb{R}, \exist \ \varepsilon>0$，使得 $\forall \  N \in \mathbb{N}, \exist \ n>N, |a_n-a|\geq \varepsilon$。

1. 对 $\varepsilon$ 的理解：==既有确定性又有任意性==

    1. 任意性：在选定 $\varepsilon$ 之前，它可以是任意一个正数。
    2. 确定性：在选定 $\varepsilon$ 之后的操作中，$\varepsilon$ 为一个确定的正数。

    > * 为了便于运算，通常将 $\varepsilon$ 取为一个较小的数，一般取 $\varepsilon \in (0, 1)$。
    > * 在多次应用极限时， ==应保持一致（唯一）==。因为 $\varepsilon$ 是一个逻辑概念，若存在多个，会引起大小混乱。
    >
2. 极限主要考量 $n\to \infty$ 的情况，即数列的末端形态。

    因此，<u>数列前面的个别项对极限无影响</u>。

#### 重要数列极限

1. $$
    \lim_{n\to \infty} {\sqrt[n]{n}} = 1
    $$
2. $$
    \lim_{n \to \infty}(1+\frac{1}{n})^n = e
    $$
3.

### 收敛数列的性质

> @info
>
> **定理 1 ：**
>
> 1. 若 $\{a_n\}$ 是收敛数列，则 $\{a_n\}$ 的极限是唯一的。
> 2. 改变数列中有限多项的值，不影响数列的收敛性及其极限。
> 3. 数列 $\{a_n\}$ 收敛于 $a$ 当且仅当它的任意子列都收敛于 $a$。

1. 定理 1.2、1.3 说明，数列的极限取决于 $n$ 充分大时，数列的形态。

> @info
>
> **定理 2 ：** （==收敛数列的形态==）设 $\{a_n\}$ 为收敛数列，极限值记为 $a$，则
>
> 1. **有界性**：$\{a_n\}$ 为有界数列，即存在一个正数 $M$（与变量 $n$ 无关），使得 $|a|\leq M$ 对所有的 $n=1,2,3,\cdots$ 成立。
> 2. **保号性**：
>
>     > 这里和今后，所谓“ $n$ 充分大”是指存在一个自然数 $N$，使得 $n>N$。
>     >
>
>     1. 若存在实数 $l$，使得 $a>l$（或 $a<l$），则当 $n$ 充分大时，有 $a_n>l$（或 $a_n<l$）。特别地，若 $a>0$（或 $a<0$），则当 $n$ 充分大时，有 $a_n>0$（或 $a_n<0$）。
>     2. 若存在实数 $l$，使得当 $n$ 充分大时，有 $a_n\geq l$（或 $a_n \leq l$），则 $a\geq l$（或 $a\leq l$）。特别地，对于充分大的 $n$，若 $a_n\geq 0$（或 $a_n\leq 0$），则 $a\geq 0$（或 $ a\leq 0$）。
>
>         * 注意：即便是严格不等式 $a_n>l$，也不一定能够保证 $a>l$。例如 $a_n=\frac{1}{n} >0$，但它的极限值却是 $0$。
> 3. $$
>     \lim_{n\to \infty}a_n=0 \Leftrightarrow \lim_{n\to \infty}|a_n|=0
>     $$

1. 由定理 2.1 可知：收敛数列 $\Rightarrow$ 有界数列，反之则不成立。

    * 收敛数列的必要条件：==收敛数列必有界==。
    * 有界不能保证收敛性，只是对数列每一项取值范围的限制，是一种宏观控制。而收敛是要求数列的通项坚定不移地无限接近一个固定的数。

> @info
>
> **定理 3 ：** （**极限号的穿越**）设 $\{a_n\}$ 和 $\{b_n\}$ 是两个<u>收敛数列</u>，即有 $\lim_{n\to \infty}a_n = a, \lim_{n\to \infty}b_n=b$，则通过四则运算形成的新的数列 $\{a_n \pm b_n\}$，$\{a_nb_n\}$，$\{\frac{a_n}{b_n}\}$ (当 $\lim_{n \to \infty} b_n \neq 0$ 时\) 都收敛，且
>
> 1. $\lim_{n\to \infty}(a_n\pm b_n)=a\pm b$。
> 2. $\lim_{n\to \infty}a_nb_n = ab$。特别地，有 $\lim_{n\to \infty}ca_n =ca$。
> 3. $\lim_{n\to \infty}\frac{a_n}{b_n} = \frac{a}{b}$，其中 $b\neq 0$。
>
>     * 关于 $b_n$ 可以作分母的解释：因为① $b\neq 0 $；② **局部保号性**，则当 $n$ 充分大时，$b_n\neq 0$。
>     * > @tip
>       >
>       > **证明：**  
>       >

1. 定理 3 的前提条件：涉及的极限都是存在的！！！
2. 扩展：对于任意有限次的四则运算，定理 3 均适用。

    * 但是，对于无限次的四则运算，因为<u>引入了描述运算次数的 </u>​<u>$n$</u>，则定理 3 不适用。
3. 应用：在计算复杂数列极限时，可以将其化为简单极限的四则运算，而不必再使用“$\varepsilon - N$”语言描述。

> @info
>
> **定理 4 ：** （极限过程的“顺序”性）设数列 $\{a_n\}$ 和 $\{b_n\}$ 分别收敛于 $a$ 和 $b$。
>
> 1. 若当 $n$ 充分大时有 $a_n\geq b_n$，则 $a\geq b$。
> 2. 若 $a>b$，则当 $n$ 充分大时，有 $a_n>b_n$。
>
> **定理 5 ：夹逼定理**——若数列 $\{b_n\}$ 和 $\{c_n\}$ 都收敛于 $a$，且对所有充分大的 $n$，有
>
> $$
> b_n\leq a_n\leq c_n,
> $$
>
> 则数列 $\{a_n\}$ 也收敛，而且极限也为 $a$。

1. 定理 5 反映了**控制思想**：将 $\{b_n\}$ 和 $\{c_n\}$ 作为“控制数列”，以描述 $\{a_n\}$ 的形态。

### 计算数列极限

#### 常规方法

1. 使用“$\varepsilon - N$”语言描述。
2. 通常需要放缩。

#### 四则运算

#### 夹逼定理

1. 寻找控制数列。

### 实数完备性若干等价命题

```mermaid
---
title: 关于实数完备性的循环论证
---
graph LR

A@{ shape: circle, label: "完备性公理" } --> 确界原理 --> 单调有界原理 --> 区间套定理
--> 列紧性定理 --> Cauthy收敛准侧 --> A@{ shape: circle, label: "完备性公理" }
```

以下的定理在有限维空间成立，而在多维空间不成立。

#### 确界原理

> @info
>
> **定义 1 ：** 设 $X\subset \mathbb{R}$ 是一个非空集合，若存在一个实数 $a$，使得对于任何 $x\in X$，有 $x\leq a$，则称 $a$ 是数集 $X$ 的一个**上界**。若存在一个实数 $b$，使得对于任何 $x\in X$，有 $x\geq b$，则称 $b$ 是数集 $X$ 的一个**下界**。若数集 $X$ 既有上界，又有下界，则称 $X$ 是**有界集合**。
>
> **定义 2 ：** 设 $a$ 是数集 $X$ 的上界，若对于任意给定的正数 $\varepsilon$，都存在一个数 $x_\varepsilon \in X$，使得 $x_\varepsilon >a-\varepsilon$，则称 $a$ 为 $X$ 的**上确界**，记为 $\sup X$。同理可定义**下确界**，记为 $\inf X$。

> @info
>
> **定理 1 ：** （**确界原理**）$\mathbb{R}$ 中任何有上（下）界的非空数集一定有上（下）确界。
>
> * > @tip
>   >
>   > **证明：** $x \in \mathbb{R}$ 是一个非空且有上界的集合，$Y$ 是 $X$ 的上界的全体，则 $X$ 在 $Y$ 的左边，由实数的完备性知：
>   >
>   > $$
>   > \exists c \in \mathbb{R}, \forall x \in x, y \in Y, x\leq c\leq y.
>   > $$
>   >
>   > 则 $c$ 是 $X$ 的最小上界，即 $c$ 是 $X$ 的上确界。
>   >

* 一个数集的上（下）确界，既可以是该数集中的数，也可以不是。

#### 单调有界原理

> @info
>
> **定义 3 ：** 数列 $\{a_n\}$，若满足 $a_n\leq a_{n+1}(n=1,2,\cdots)$，则称为**单调(递)增的**；若满足 $a_n\geq a_{n+1}(n=1,2,\cdots)$，则称为**单调(递)减的**。单调递增和单调递减数列，通称**单调数列**。

> @note
>
> **定理 2：** （**单调有界原理**）单调增（减）有界数列必收敛，其极限值等于数列构成的数集的上确界（或下确界），也称为**数列的上确界（或下确界）** 。
>
> * > @tip
>   >
>   > **证明：** 设数列 $\{a_n\}$ 单调递增有上界，因此根据[确界原理](siyuan://blocks/20251018114548-xj39s1f)，数集
>   >
>   > $$
>   > X = \{a_1, a_2, \cdots, a_n, \cdots \}
>   > $$
>   >
>   > 必有上确界，记上确界为 $a$，即 $a = \sup\{a_n\}$。因此，$\forall \varepsilon >0$，$\exist N \in \mathbb{N}_+$，使 $ a_N >a-\varepsilon$。又因为 $\{a_n\}$ 单调递增，所以 $\forall n>N$，有 $a_n \geq a_N > a-\varepsilon$。
>   >
>   > 显然 $a+\varepsilon > a \geq  a_n$ 对任何 $n$ 成立。所以当 $n>N$ 时，有 $a+\varepsilon > a_n > a-\varepsilon$， 即 $|a_n-a| < \varepsilon$，即 $\lim a_n = a$。
>   >
>
> **定理 3 ：** 设 $e_n = (1+\frac{1}{n})^n, n\geq 1$，则数列 $\{e_n\}$ 收敛，并记数列 $\{e_n\}$ 的极限为
>
> $$
> e = \lim_{n \to \infty}(1+\frac{1}{n})^n.
> $$
>
> * > @tip
>   >
>   > **证明：** 首先证明该数列是递增的。事实上，由二项式定理可得
>   >
>   > $$
>   > \begin{equation}
>   >   \begin{align*}  
>   >      e_n &= 1+\sum_{k=1}^n C_n^k \cdot \frac{1}{n^k}  \\
>   >          &= 1+\sum_{k=1}^n \frac{1}{k!} \cdot \frac{n(n-1)(n-2)
>   > \cdots(n-k-1)}{n^k}  \\
>   >          &= 1+1+\sum_{k=2}^n \frac{1}{k!} (1-\frac{1}{n})(1-\frac{2}{n})
>   > \cdots (1-\frac{k-1}{n}), 
>   >   \end{align*}
>   > \end{equation}
>   > $$
>   >
>   > $$
>   > \begin{equation}
>   >   \begin{align*}  
>   > 	e_{n+1} &= 1+1+\sum_{k=2}^n \frac{1}{k!} (1-\frac{1}{n+1})
>   > (1-\frac{2}{n+1}) \cdots (1-\frac{k-1}{n+1}) + (\frac{1}{n+1})^{n+1}.
>   >   \end{align*}
>   > \end{equation}
>   > $$
>   >
>   > 比较 $e_n$ 和 $e_{n+1}$ 两个表达式的右端和号中的对应项，显然，前者较小。而 $e_{n+1}$ 所多出来的一项 $(\frac{1}{n+1})^{n+1}>0$，故 
>   >
> * 该定理给出了**自然常数** $e$ 的定义。

1. 在实际运用中，（单调递增+上界）or（单调递减+下界）$\Rightarrow$ 收敛。
2. 使用步骤：

    1. 由 单调有界原理 知极限存在。
    2. 通过对递推式两端求极限，得 $a$。

    > @example
    >
    >
    > **例：** $a_n = \underbrace{\sqrt{2+\sqrt{2+\sqrt{\cdots +\sqrt{2}}}}}_{n重开方}$，求 $a_n$ 的极限。
    >
3. 定理 3 的延伸结论：

    * 有理数域对极限运算是不封闭的，因此有必要对其进行扩充，使之成为一个完备（连续）的数域。
    * 事实上，利用有理数列的极限可以构造所有无理数。

#### 区间套定理

> @info
>
> **定理 4 ：** （**区间套定理**）设有一列<u>闭区间</u> $[a_n, b_n], n=1,2,\cdots$，满足下列条件：
>
> 	1°$[a_1, b_1] \subset [a_2, b_2] \subset \cdots [a_n, b_n] \subset \cdots$。
>
> 	2°$\lim_{n\to \infty}(b_n-a_n)=0$。
>
> 则存在唯一一个点 $\xi$ 属于所有闭区间 $[a_n, b_n], n=1,2,\cdots$。
>
> * > @tip
>   >
>   > **证明：** ①证明 $\{a_n\}$、$\{b_n\}$收敛；②证明两个数列极限一致。
>   >
>   > * 思考：如何证明 $\xi$ 的唯一性？
>   >

1. 使用区间套的条件：一定是闭区间，<u>不能包含开区间或半开半闭区间</u>。
2. 符号 $\bigcap_{n=1}^{\infty}[a_n, b_n]$：表示无限多个闭区间的交集。

#### 列紧性定理

> @info
>
> **定理 5 ：** （**Bolzano-Weierstrass 定理**）任何<u>有界</u>的数列必存在收敛的子列。

#### Cauthy 收敛准侧

> @info
>
> **定义 4 ：** 对于数列 $\{a_n\}$，
>
> 方式一：若 $\forall \varepsilon >0$，$\exist N\in \mathbb{N}, \ N=N(\varepsilon)$，使得 $\forall m,n>N$ ，有$|a_n - a_m|<\varepsilon$，
>
> 方式二：若 $\forall \varepsilon >0$，$\exist N\in \mathbb{N}$，使得 $\forall n>N, p\in \mathbb{N}$，有 $|a_{n+p} - a_n |< \varepsilon$，
>
> 则称该数列为**基本列**。

> @info
>
> **定理 6 ：** （**Cauthy 收敛准则**）数列 $\{a_n\}$ 收敛的充分必要条件是 $\{a_n\}$ 是基本列。

1. 基础：实数完备性——在实数集内，数列是完备的。
2. 特殊之处：在不借助外界信息的情况下，仅根据数列自身内在性态来判断数列的收敛性。即，==不需要知道极限值，就可以对极限进行==​==<u>定义</u>==​==。==
3. 自然地从 [Cauthy 收敛准则](siyuan://blocks/20251018171127-u2cdl7z) $\Rightarrow$ [实数完备性](siyuan://blocks/20251007191813-wek6fg8)：

    ![收敛准则推导实数完备性](数学分析20250925091413w6cr839.md.收敛准则推导实数完备性-20251018172025-y4t2xfn.jpg)

    通过**循环论证**，即可证明以上定理的等价性。
4. > @note
    >
    > **推论：** 设数列 $\{a_n\}$ 满足 $\exist M>0$，$|a_n|\leq \frac{M}{n^2}$，则它的部分和 $S_n = a_1+a_2+\cdots+a_n$ 是一个收敛数列。
    >

### 发散到无穷大的数列

> @info
>
> **定义 1 ：** 设 $\{a_n\}(n\geq 1)$ 是给定的数列，若 $\forall M>0$，$\exist N\in \mathbb{N}$，使 $\forall n>N$，有 $|a_n|>M$，则称**数列** $\{a_n\}(n\geq 1)$ **发散到无穷大**（或称**数列趋于无穷大**），记作
>
> $$
> \lim_{n\to \infty}a_n = \infty  \ \ \ 或\ \ \  a_n \to \infty(n\to \infty).
> $$
>
> 1. 将 $|a_n|>M$ 换为 $a_n>M$，则可定义“**发散到正无穷大**（**趋于正无穷大**）”：$a_n \to +\infty(n\to \infty)$。
> 2. 将 $|a_n|>M$ 换为 $-a_n>M$，则可定义“**发散到负无穷大**（**趋于负无穷大**）”：$a_n \to -\infty(n\to \infty)$。
>
> **定义 2 ：** 如果数列 $\{a_n\}(n\geq 1)$ 发散到无穷大，那么称当 $n$ 趋于无穷时 $a_n$ 是**无穷大量**。对应的，如果数列 $\{a_n\}(n\geq 1)$ 收敛于 0，那么称当 $n$ 趋于无穷时 $a_n$ 是**无穷小量**。

> @info
>
> **定理 1 ：** 单调数列发散到无穷大的充分必要条件是其为一个无界数列。

1. 注意：

    1. 发散到无穷大的数列是发散数列。
    2. 发散数列未必发散到无穷大。

### Stolz 定理

> @info
>
> **Stolz（施托尓兹定理）** 是在求数列的极限时常用的一种方法。
>
> **定理 1 ：** （**$\frac{\infty}{\infty}$** **型 Stolz 定理**）设 $\{a_n\}$，$\{b_n\}$ 是两个数列，且 $\{b_n\}$ 严格递增趋于 $+\infty$。如果
>
> $$
> \lim_{n\to\infty} \frac{a_{n+1}-a_n}{b_{n+1}-b_n}=A,
> $$
>
> 则有
>
> $$
> \lim_{n\to\infty} \frac{a_n}{b_n} = A,
> $$
>
> 其中 $A$ 可以是实数，也可以是 $+\infty$ 或 $-\infty$。
>
> **定理 2 ：** （**$\frac{0}{0}$** **型 Stolz 定理**）设 $\{a_n\}$，$\{b_n\}$ 是两个收敛于 0 的数列，且 $\{b_n\}$ 是严格递减数列。如果
>
> $$
> \lim_{n\to\infty} \frac{a_{n+1}-a_n}{b_{n+1}-b_n}=A,
> $$
>
> 则有
>
> $$
> \lim_{n\to\infty} \frac{a_n}{b_n} = A,
> $$
>
> 其中 $A$ 可以是实数，也可以是 $+\infty$ 或 $-\infty$。

1. 理解：可以将 Stolz 定理看作一种离散形式的 L'Hospital 法则。
2. 注意事项：

    1. $\frac{\infty}{\infty}$ 型 Stolz 定理的证明中<u>并没有要求数列 </u>​<u>$\{a_n\}$</u>​<u> 一定发散到 </u>​<u>$\infty$</u>，只是为了与 $\frac{0}{0}$ 型 Stolz 定理对应，采用了记号 $\frac{\infty}{\infty}$。
    2. 两种类型的 Stolz 定理的<u>逆命题并不成立</u>。
    3. Stolz 定理中的 <u>$A$</u>​<u> 不可以是 </u>​<u>$\infty$</u>，即其正负性必须一定。
